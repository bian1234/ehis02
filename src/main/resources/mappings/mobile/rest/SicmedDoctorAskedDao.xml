<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.cfq.mobile.rest.dao.SicmedDoctorAskedDao">
    
	<sql id="sicmedDoctorAskedColumns">
		a.id AS "id",
		a.doctor_code AS "sicmedUser.id",
		a.asked_class_code AS "sicmedAskedClass.id",
		a.asked_code AS "sicmedAsked.id",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="sicmedDoctorAskedJoins">
		LEFT JOIN sicmed_user b ON b.id = a.doctor_code
		LEFT JOIN sicmed_asked_class c ON c.id = a.asked_class_code
		LEFT JOIN sicmed_asked d ON d.id = a.asked_code
	</sql>
    
	<select id="get" resultType="SicmedDoctorAsked">
		SELECT 
			<include refid="sicmedDoctorAskedColumns"/>
		FROM sicmed_doctor_asked a
		<include refid="sicmedDoctorAskedJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="SicmedDoctorAsked">
		SELECT 
			<include refid="sicmedDoctorAskedColumns"/>
		FROM sicmed_doctor_asked a
		<include refid="sicmedDoctorAskedJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="SicmedDoctorAsked">
		SELECT 
			<include refid="sicmedDoctorAskedColumns"/>
		FROM sicmed_doctor_asked a
		<include refid="sicmedDoctorAskedJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO sicmed_doctor_asked(
			id,
			doctor_code,
			asked_class_code,
			asked_code,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{sicmedUser.id},
			#{sicmedAskedClass.id},
			#{sicmedAsked.id},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE sicmed_doctor_asked SET 	
			doctor_code = #{sicmedUser.id},
			asked_class_code = #{sicmedAskedClass.id},
			asked_code = #{sicmedAsked.id},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE sicmed_doctor_asked SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>