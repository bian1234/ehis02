<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.cfq.mobile.rest.dao.SicmedInspectClassDao">
    
	<sql id="sicmedInspectClassColumns">
		a.id AS "id",
		a.inspect_name AS "inspectName",
		a.inspect_code AS "inspectCode",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="sicmedInspectClassJoins">
	</sql>
    
	<select id="get" resultType="SicmedInspectClass">
		SELECT 
			<include refid="sicmedInspectClassColumns"/>
		FROM sicmed_inspect_class a
		<include refid="sicmedInspectClassJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="SicmedInspectClass">
		SELECT 
			<include refid="sicmedInspectClassColumns"/>
		FROM sicmed_inspect_class a
		<include refid="sicmedInspectClassJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<!-- <if test="inspectName != null and inspectName != ''">
				AND a.inspect_name = #{inspectName}
			</if>
			<if test="inspectCode != null and inspectCode != ''">
				AND a.inspect_code = #{inspectCode}
			</if> -->
			<if test="inspectName != null and inspectName != ''">
				AND a.inspect_name = #{inspectName}
			</if>
			<if test="inspectCode != null and inspectCode != ''">
				AND a.inspect_code = #{inspectCode}
			</if>
			<if test="createBy != null and createBy.id != null and createBy.id != ''">
				AND a.create_by = #{createBy.id}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<!-- 模糊查询带分页web -->
	<select id="findLike" resultType="SicmedInspectClass">
		SELECT 
			<include refid="sicmedInspectClassColumns"/>
		FROM sicmed_inspect_class a
		<include refid="sicmedInspectClassJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="inspectName != null and inspectName != ''">
				AND a.inspect_name LIKE CONCAT('%',#{inspectName},'%')
			</if>
			<if test="inspectCode != null and inspectCode != ''">
				AND a.inspect_code LIKE CONCAT('%',#{inspectCode},'%')
			</if>
			<if test="createBy != null and createBy.id != null and createBy.id != ''">
				AND a.create_by = #{createBy.id}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="SicmedInspectClass">
		SELECT 
			<include refid="sicmedInspectClassColumns"/>
		FROM sicmed_inspect_class a
		<include refid="sicmedInspectClassJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO sicmed_inspect_class(
			id,
			inspect_name,
			inspect_code,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{inspectName},
			#{inspectCode},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE sicmed_inspect_class SET 	
			inspect_name = #{inspectName},
			inspect_code = #{inspectCode},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE sicmed_inspect_class SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>